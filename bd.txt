-- Script para criação das tabelas e relacionamentos

-- Tabela projeto
CREATE TABLE projeto (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL
);

-- Tabela linha
CREATE TABLE linha (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL
);

-- Tabela cargo
CREATE TABLE cargo (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL
);

-- Tabela funcao
CREATE TABLE funcao (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL
);

-- Tabela turno
CREATE TABLE turno (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL
);

-- Tabela funcionario
CREATE TABLE funcionario (
    matricula VARCHAR(20) PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL,
    whatsapp VARCHAR(15),
    telefone VARCHAR(15),
    data_admissao DATE NOT NULL,
    onibus VARCHAR(10),
    ponto_referencia VARCHAR(100),
    cep VARCHAR(9),
    logradouro VARCHAR(100),
    numero VARCHAR(10),
    bairro VARCHAR(50),
    cidade VARCHAR(50),
    estado VARCHAR(2),
    projeto_id INT,
    linha_id INT,
    cargo_id INT,
    funcao_id INT,
    turno_id INT,
    FOREIGN KEY (projeto_id) REFERENCES projeto(id),
    FOREIGN KEY (linha_id) REFERENCES linha(id),
    FOREIGN KEY (cargo_id) REFERENCES cargo(id),
    FOREIGN KEY (funcao_id) REFERENCES funcao(id),
    FOREIGN KEY (turno_id) REFERENCES turno(id)
);


tabela projeto: GEM, SPIN, VS30, BMW - SEAT, BMW - WIRE, RENAULT
tabela linha: (GEM: IP1,IP2,IP3,BODY1,BODY2,B0DY3,BODY4,ENGINE1,ENGINE2,SMALLS)(SPIN: IP, BODY, ENGINE, SMALLS)(VS30:VORNE)
tabela cargo: Lider, Operador, Supervisor, Mult 1, Mult 2, Inspetor de Qualidade
tabela funcao: Acabamento, Conectagem - GTI, Conectagem Rack, Conectagem Roteamento, MTE, MTC, MTEst, Care, Embalagem, Enrolado, EPC, N/A
tabela turno: 1º Turno, 2º Turno, 3º Turno, Comercial

